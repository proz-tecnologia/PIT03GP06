//import 'package:ctrl_real/src/model/totallandcategory_model.dart';
//import 'package:uuid/uuid.dart';

void main() {
  /*final trans = TotalandCategory(
      id: const Uuid().v4(),
      date: "",
      type: "",
      formPag: "",
      valor: 2500,
      descri: "");*/

      //testWidgets("Teste transações", (widgetTester) async {
        //await widgetTester.pumpWidget(MultiProvider(providers: [ChangeNotifierProvider(create: TransactionsController(authentinc: u))]))
      //},);
}
